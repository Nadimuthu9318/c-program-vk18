#include <stdio.h>

int main() {
    int matrix[3][3];
    int i, j;

    // Read 3x3 matrix elements
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 3; j++) {
            scanf("%d", &matrix[i][j]);
        }
    }

    // Print the matrix
    for(i = 0; i < 3; i++) {
        for(j = 0; j < 3; j++) {
            printf("%d", matrix[i][j]);
            if(j < 2) {
                printf(" ");
            }
        }
        printf("\n");
    }

    return 0;
}
