#include <stdio.h>


void attemptSwap(int x, int y) {
    int temp = x;
    x = y;
    y = temp;
  
    printf("After Swap: %d %d\n", x, y);
}

int main() {
    int a, b;

 
    if (scanf("%d %d", &a, &b) != 2) return 0;

    printf("Before Swap: %d %d\n", a, b);

    
    attemptSwap(a, b);

    return 0;
}
